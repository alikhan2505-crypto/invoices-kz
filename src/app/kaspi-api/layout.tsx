import ProductShell from '@/components/ProductShell'

export default function KaspiApiLayout({ children }: { children: React.ReactNode }) {
  return <ProductShell product="kaspiApi">{children}</ProductShell>
}
