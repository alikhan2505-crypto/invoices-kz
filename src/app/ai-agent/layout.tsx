import ProductShell from '@/components/ProductShell'

export default function AiAgentLayout({ children }: { children: React.ReactNode }) {
  return <ProductShell product="aiAgent">{children}</ProductShell>
}
